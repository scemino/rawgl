fips_begin_app(raw windowed)
    add_definitions(-DBYPASS_PROTECTION)
    fips_files(raw.c game-impl.cc
    bitmap.cpp file.cpp sokol_sys.cc unpack.cpp util.cpp)
    fips_deps(gfx sokol)
    if (FIPS_EMSCRIPTEN)
    else()
        fips_libs(z)
    endif()
    fips_deps(raw-data)
fips_end_app()

fips_begin_app(raw-ui windowed)
    add_definitions(-DBYPASS_PROTECTION)
    fips_files(raw.c game-impl.cc raw-ui-impl.cc
    bitmap.cpp file.cpp sokol_sys.cc unpack.cpp util.cpp)
    fips_deps(gfx sokol)
    if (FIPS_EMSCRIPTEN)
    else()
        fips_libs(z)
    endif()
    fips_deps(raw-data ui)
fips_end_app()
target_compile_definitions(raw-ui PRIVATE GAME_USE_UI)